package com.example.rpgaudiomixer.app.theme

import androidx.compose.ui.graphics.Color

// Backgrounds
val ArcanumBlack = Color(0xFF080808)
val ArcanumSurface = Color(0xFF111111)
val ArcanumSurfaceVariant = Color(0xFF1A1A1A)
val ArcanumCard = Color(0xFF151515)

// Gold / Amber accents
val ArcanumGold = Color(0xFFFFBE45)
val ArcanumGoldDim = Color(0xFFCC9020)
val ArcanumAmber = Color(0xFFFFA500)
val ArcanumOnGold = Color(0xFF1A0E00)

// Purple / Pink - for gradients & secondary
val ArcanumPurple = Color(0xFF9B59B6)
val ArcanumPurpleLight = Color(0xFFBE8FD0)
val ArcanumPink = Color(0xFFE91E8C)
val ArcanumPinkLight = Color(0xFFFF6BE8)

// Slider gradient stops (purple -> pink -> gold)
val SliderStart = ArcanumPurple
val SliderMid = ArcanumPink
val SliderEnd = ArcanumGold

// Text / icons
val ArcanumOnSurface = Color(0xFFEEE8D0)
val ArcanumOnSurfaceVariant = Color(0xFF9A9080)
val ArcanumGlow = Color(0xFFFFBE45).copy(alpha = 0.35f)

// Disabled
val ArcanumDisabled = Color(0xFF3A3530)
val ArcanumDisabledContent = Color(0xFF5A5550)